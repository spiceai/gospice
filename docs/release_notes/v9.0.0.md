# Announcing gospice v9.0.0 - Async Queries & DataFusion 54 🚀

gospice v9.0.0 introduces **asynchronous queries** and upgrades the Apache Arrow stack. It also adds mutual TLS, semantic search, per-component runtime status, active-query management, and natural-language querying. This is a major release with breaking changes.

## 🎯 Major Features

### Async Queries

`Query` and `QueryWithParams` now submit SQL for **asynchronous** execution and return an `*AsyncQuery` handle. Async queries run in the background on the Spice runtime and are designed for long-running analytical and batch workloads. They are served over the Arrow Flight async actions API and require the runtime to be running in distributed/scheduler mode (`spiced --role scheduler` with `runtime.scheduler.state_location` configured).

```go
query, err := spice.Query(ctx, "SELECT * FROM taxi_trips")
if err != nil {
    panic(err)
}
fmt.Println("query id:", query.ID())

// Wait for completion and fetch results as an Apache Arrow reader
reader, err := query.Results(ctx)
if err != nil {
    panic(err)
}
defer reader.Release()

for reader.Next() {
    fmt.Println(reader.RecordBatch())
}
```

The `*AsyncQuery` handle exposes `ID()`, `Status(ctx)`, `Wait(ctx)`, `Results(ctx)`, and `Cancel(ctx)`. Status values are `PENDING`, `RUNNING`, `SUCCEEDED`, `FAILED`, `CANCELLED`, and `CLOSED`.

### DataFusion 54 Arrow stack

gospice v9 upgrades to the Apache Arrow releases that match the Spice.ai runtime's DataFusion 54 (Rust arrow-rs 58.3.0):

- **Apache Arrow Go** `v18.4.1` → `v18.6.0`
- **ADBC Go driver** `v1.9.0` → `v1.11.0`

### Mutual TLS (mTLS)

`Init` accepts two new options for securing the connection to the runtime:

- `WithTLSClientCertificate(certFile, keyFile)` presents a PEM-encoded client certificate during the TLS handshake for mTLS authentication.
- `WithTLSRootCertificate(caFile)` verifies the server against a custom CA certificate instead of (or in addition to) the system certificate store.

### Search

`Search` finds documents similar to a piece of text via the runtime's `/v1/search` endpoint, against datasets with an embedding column and a loaded embedding model. It supports restricting to specific datasets, a SQL `Where` filter, additional returned columns, and a hybrid lexical+vector ranking via `Keywords`.

### Runtime Status

`RuntimeStatus` reports per-component status (`http`, `flight`, `metrics`, `opentelemetry`) via `/v1/status`, distinguishing a runtime that is still initializing from one whose Flight endpoint is failing — unlike `IsSpiceReady`, which reports a single boolean for the whole runtime.

### Listing and Cancelling Running Queries

`ListActiveQueries` reports the synchronous queries running in the caller's scope (from `Sql`, `SqlWithParams`, FlightSQL, NSQL, and `Search`), and `CancelActiveQuery` stops one by ID — the runtime never hands a query's ID back to the client that submitted it, so these are meant to be used together. See the README for the scope and runtime-version caveats that apply.

### Text-to-SQL (NSQL)

`Nsql` answers a natural-language question by having the runtime's configured LLM generate SQL and run it, returning both the rows and the generated query. `NsqlGenerateSQL` generates the SQL without running it, so it can be inspected, edited, or run through `Query`/`Sql` for Arrow-typed results. Both call the runtime's `/v1/nsql` endpoint and require an LLM model configured in the Spicepod.

## 📦 What's Changed

### Breaking Changes

- **`Query` / `QueryWithParams` are now asynchronous** and return `*AsyncQuery` instead of `array.RecordReader`. Use `Sql` / `SqlWithParams` for synchronous, streaming queries, or use the async handle's `Wait()` / `Results()`.
- **Minimum Go version is now 1.25** (was 1.24), required by the upgraded Arrow dependencies.
- **Numbers in `Search` and `Nsql` result maps are `json.Number`, not `float64`.** This affects `SearchMatch.Matches`/`PrimaryKey`/`Data`/`Metadata` and `NsqlResponse.Data`, whose values are interface-typed. A `float64` type assertion on one of those values must become `json.Number` followed by `Int64()`, `Float64()`, or `String()`. Typed struct fields such as `SearchMatch.Score` and `NsqlResponse.RowCount` are unaffected.

### Added

- `AsyncQuery` handle type with `ID`, `Status`, `Wait`, `Results`, and `Cancel`.
- `QueryStatus` type and constants.
- `WithTLSClientCertificate` and `WithTLSRootCertificate` client options for mTLS.
- `Search` and `SearchRequest`/`SearchResponse`/`SearchMatch`.
- `RuntimeStatus` and `ConnectionDetails`/`ComponentStatus`.
- `ListActiveQueries` and `CancelActiveQuery`, and the `ActiveQuery` type.
- `Nsql` and `NsqlGenerateSQL`, and the `NsqlRequest`/`NsqlResponse`/`NsqlSchema`/`NsqlField` types.

### Fixed

- **ADBC connections re-authenticate instead of failing permanently.** `SqlWithParams` previously treated an `Unauthenticated` error from an expired server-side session as permanent, leaving the connection unusable until the process restarted. It now re-opens the ADBC connection and retries the query once. Re-authentication no longer triggers on `Unauthorized`, which means the credential is valid but lacks permission — a fresh handshake cannot fix that.
- **Re-authentication no longer disrupts concurrent queries.** Replacing the ADBC connection closed it immediately, so a query running on another goroutine — or a `RecordReader` already returned to a caller and still streaming — could fail mid-flight with a use-after-close. A replaced connection is now retired and closed only once the last query and reader using it has finished.
- **mTLS and custom CA certificates now apply to `SqlWithParams`.** The ADBC FlightSQL connection was built without the client certificate, private key, and root CA that `WithTLSClientCertificate` and `WithTLSRootCertificate` configure, so `Init` and every HTTP method could reach an mTLS or private-CA runtime while `SqlWithParams` failed TLS verification. Configuring either now also forces the `grpc+tls://` scheme.
- **64-bit values in `Search` and `Nsql` results are no longer rounded.** Values in interface-backed maps were decoded as `float64`, silently losing precision above 2^53 — a primary key of `9007199254740993` came back as `9007199254740992`, quietly naming the wrong row.
- **An unset API key is no longer sent as an empty one.** `Search` and `RefreshDataset` set the `X-API-Key` header unconditionally, so a client with no key configured sent an empty header value. Auth middleware reads that as a supplied-but-invalid credential rather than an absent one, which broke runtimes configured to allow anonymous access. The header is now omitted entirely when no key is configured, matching every other method.

### Changed

- Module import path `github.com/spiceai/gospice/v8` → `github.com/spiceai/gospice/v9`.
- Upgraded `github.com/apache/arrow-go/v18` to `v18.6.0`.
- Upgraded `github.com/apache/arrow-adbc/go/adbc` to `v1.11.0`.

### Unchanged

- `Sql()` / `SqlWithParams()`, `IsSpiceHealthy()` / `IsSpiceReady()`, client initialization, `RefreshDataset()`, and the typed parameter constructors are all source-compatible with v8.

## 🔧 Migration Guide

See [UPGRADE_V8_TO_V9.md](../../UPGRADE_V8_TO_V9.md) for detailed migration instructions.

```bash
go get github.com/spiceai/gospice/v9@latest
go mod tidy
```

```go
// Before
import "github.com/spiceai/gospice/v8"

// After
import "github.com/spiceai/gospice/v9"
```

## 📞 Support

- 📖 Documentation: [docs.spice.ai](https://docs.spice.ai/sdks/go)
- 🐛 Issues: [github.com/spiceai/gospice/issues](https://github.com/spiceai/gospice/issues)
- 💬 Discord: [spice.ai/discord](https://spice.ai/discord)

**Full Changelog**: <https://github.com/spiceai/gospice/compare/v8.0.1...v9.0.0>
